# Airflow image dependencies
# Base image: apache/airflow:2.8.1-python3.10 (includes airflow core)

apache-airflow==2.8.1
apache-airflow-providers-google>=10.13.0
apache-airflow-providers-slack>=8.5.0

# Data processing (used by DAG task callables and ingestion scripts)
pandas>=2.0.0
pyarrow>=14.0.0
gcsfs>=2024.1.0

# Jolpica API ingestion
requests>=2.31.0
tenacity>=8.2.0

# FastF1 (optional — DAG degrades gracefully if not present)
fastf1>=3.3.0

# Misc
python-dotenv>=1.0.0
