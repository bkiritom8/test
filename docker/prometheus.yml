global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'f1-strategy-optimizer'

scrape_configs:
  # F1 Strategy API metrics
  - job_name: 'api'
    static_configs:
      - targets: ['api:8000']
    metrics_path: /metrics

  # Prometheus itself
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
